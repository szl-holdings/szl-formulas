[project]
name = "szl-formulas"
version = "0.1.0"
description = "Canonical formula registry and governed-loop composer. Software kernel, not a trained model."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
